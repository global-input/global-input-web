import React from 'react';
import {styles} from './styles';

const SubtitleSectionContainer=props=>(
          <div style={styles.container}>
              {props.children}
          </div>
        );

export default SubtitleSectionContainer;
