import React from 'react';
import {Helmet} from 'react-helmet'
export default ({title="Global Input App - Introducing Mobile Interoperability to Web and Device Applications"})=>(
  <Helmet>
        <title>{title}</title>
  </Helmet>
);