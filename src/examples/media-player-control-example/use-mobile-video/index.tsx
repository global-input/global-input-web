import React, { useRef, useState,useEffect} from "react";
import { useGlobalInputApp } from 'global-input-react';

export default (firstVideo) => {
    const [videoData,setVideoData]=useState(firstVideo);
    

};

